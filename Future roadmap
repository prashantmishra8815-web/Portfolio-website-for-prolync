“This is only the first version. In the next phase I plan to convert it into a React app, add a backend for the contact form, implement a dark/light mode toggle, and set up automatic deployment with analytics. This ensures the site remains fast, accessible, and scalable.”

